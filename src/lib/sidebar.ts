/** Cookie the sidebar writes client-side and the (app) layout reads on the server,
 *  so the rail renders in its last-used state on first paint (no width flash). */
export const SIDEBAR_COOKIE = "hgos-sidebar-pinned";
